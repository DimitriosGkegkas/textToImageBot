import { IFrame } from "@ebenos2/viber-elements";
import colors from "./colors";

const buttonFrame: IFrame = {
    BorderWidth: 5,
    BorderColor: colors.lessDarkBlue,
    CornerRadius: 10,
};

export const options = { Frame: buttonFrame };
